# Database
DATABASE_URL="file:./prisma/dev.db"

# NextAuth
# Generate with: openssl rand -base64 32
NEXTAUTH_SECRET="your-secret-key-here"
NEXTAUTH_URL="http://localhost:3000"

# Encryption key for PII data (FERPA compliance)
# Generate with: openssl rand -hex 32
ENCRYPTION_KEY="your-64-character-hex-key-here"

# WebAuthn (Passkey) Configuration
# RP ID should be your domain (without protocol or port)
WEBAUTHN_RP_ID="localhost"
WEBAUTHN_RP_NAME="Classroom Check-In"
# Origin must include protocol and port if non-standard
WEBAUTHN_ORIGIN="http://localhost:3000"

# Google OAuth (for Google Classroom import)
# Create credentials at https://console.cloud.google.com/apis/credentials
# Enable Google Classroom API at https://console.cloud.google.com/apis/library/classroom.googleapis.com
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"
